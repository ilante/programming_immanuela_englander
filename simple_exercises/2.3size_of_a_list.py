# # 2.3 Size of a list
# Given a list of numbers l, count the number of elements it contains.
# Please notice that you cannot use the built-in function len, neither the function range.
# Hint: you should evaluate the following function:
l=[2,8,'e','cat',5,6, 'nix', 'respect', 'justice']
index=0
for element in l:
    index = index+1
    element=index
print(element)